<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('style') }}
        {% endblock %}
    </head>
    <body>
    {% block navbar %}
        <div class="navbar navbar-expand-lg navbar-light bg-light mb-5">
            <p class="textStyle">Administration</p>
            <a class="nav-link" href="{{ path('home') }}">Home</a>
            <a class="nav-link" href="{{ path('etudiant_index') }}">Etudiants</a>
            <a class="nav-link" href="{{ path('projet_index') }}">Projets</a>
            <a class="nav-link" href="{{ path('intervenant_index') }}">Intervenants</a>
            <a class="nav-link" href="{{ path('matiere_index') }}">Matieres</a>
        </div>
    {% endblock %}
        <div class="container containerStyle">
            {% block body %}{% endblock %}
        </div>
        <div class="footer container">
            {% block footer %}
                Léonie Grimoin - 15 janvier 2020
            {% endblock %}
        </div>
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
